# quikdev-challenge
QuikDev back-end challenge



yarn add --dev jest
yarn add express
yarn add cors
yarn add dotenv


package.json 

 "scripts": {
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node"
  }

yarn run test

